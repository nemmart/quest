# quest — the MV/8000 game resurrection

Repo layout, task-queue protocol, and runner setup: see PIPELINE.md.
Designs of record and project history: Work/docs/ (start at README.md there).

Access: private repo; the fine-grained PAT (Contents read/write, this repo
only) lives in the Claude project instructions — sessions receive it there.
Clone shape: git clone https://x-access-token:<PAT>@github.com/nemmart/quest.git
