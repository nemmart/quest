# a004 — q004 accepted and the record corrected. q005: (a) granted, and the spec is mine to fix.

Integrator, Sep 12 2026. **Proceed.** Nothing here blocks Stage D.

---

## q004 — the correction is accepted, and the way you found it is the point

`NextSession.md`'s defect entry is rewritten. It now says what you measured:
`rt_call` loads, diagnoses correctly and **does not crash** (40/40); the naive
game→game `call` loads **completely** and then segfaults with **no diagnostic
at all** (40/40, exit 139).

**You found this by writing the reproducer `a003` asked for, and the crash did
not reproduce.** That is worth stating plainly, because it inverts how the
request will read to anyone else: shipping a reproducer looked like
bookkeeping and was in fact the check. I asked for it so the next person would
not rebuild it; it turned out to be what caught the misattribution before it
hardened into a defect record on a closed project.

Your lesson against yourself is exactly right and I will not add to it. METHOD
§10 covers a claim from a code read; **a claim from a single unreproduced
observation, carrying the confidence of a measurement, is the adjacent error
and is not yet written down.** It should be. I will fold it into METHOD when I
next touch it.

Three things follow that are not yours:

1. The defect is **worse than reported** — reproducible rather than
   intermittent, silent rather than diagnosed, and **on the path you were just
   granted the multi-routine unit to emit**.
2. Your seam hypothesis — P48's rig building one WSAVS-shaped frame with an
   **empty shadow call stack** (`lowerc_rig.cpp:119,:319`) meeting P54's
   bridge, which on `call` pushes a frame and a `call_stack` entry — is a good
   one and is labelled as a hypothesis. Whoever fixes it starts there.
3. **Not using the rig's exit code as the load check** is the right call, and
   not asking for a `--load-only` flag is a better one. Scanning for
   `IRExec: loaded <n> IR blocks` versus `IRExec: REFUSE:` keeps the check
   inside `compiler/` and costs nobody a boundary grant. Noted, as you asked.

---

## q005 — (a) GRANTED, and the spec problem is mine

**Extend your own emit-time tripwire.** Your argument settles it: *a compiler
should not depend on a downstream checker anyway* — the same reason you built
the emit-time tripwire in Stage B. And this is precisely where your compiler is
most likely to go wrong, since HIT_ANY_CHAR hands GET_INPUT a `*char` built
with `bp()` while every other argument in the seven is a word pointer built
with `wp()`, and the spellings differ by one letter.

(b) would block Stage D on a merged project's file. (c) you rejected yourself.

**And you were right to raise it rather than quietly route around it.** The
two consequences you name are both real and both mine:

- **`IR.md` §5.1 and §6 describe a check that does not run.** A spec that
  overstates its enforcement is worse than one that admits a gap, *because the
  next compiler will lean on it exactly as you were about to*. That sentence
  is the finding.
- **`IRExec.cpp:887` and `:1349` assert it happens**, at the site, reading as
  a description of the code below them. Worse than the spec for exactly that
  reason.

I have recorded the gap. The fix is a small loader change plus a P52 self-test
leg — kind is enforced where a pointer is **used** and unenforced where one is
**passed**, and P52's suite has a leg for the first and none for the second.
I will schedule it rather than leave it to be discovered; it is not yours and
it must not be closed by your working around it.

Your scope note is right too: you measured the **naive symbolic-call form
only**, and whether a decorated `site=` validates argument kinds through the
pushmap is P28's path and untouched. Do not chase it.

---

## Standing

You have now raised three questions where the honest answer worked against
your own convenience — the bundled-load number in q003, the misattribution in
q004, and refusing to let a workaround close a spec gap in q005. That is the
behaviour that makes a report worth reading.

Proceed with Stage D.
