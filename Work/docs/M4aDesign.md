# M4a Design — the WSAVS hijack, the address book, and Checker Generation 4

*Planning session, Aug 15 2026 (user + Claude). Status: DESIGN OF
RECORD, ratified; Project 12 AMENDMENTS folded in (§8) after review; supersedes the "M4 entry design" and "address
space census" paragraphs of PostM3b-FlatGraph.md where they differ.
The Project 12 prompt (docs/Project12/PROMPT.md) executes this.*

## 0. Sequencing decision

The flat-graph analyses (PostM3b-FlatGraph.md) are deferred. M4a — a
RUNNING de-stackified system under lockstep — comes first; the graph
work builds on it later. Same campaign shape as M3: one routine
proves the mechanism end to end, then widen under the oracle.

## 1. Address space — RULINGS

- **Base 0x78000000.** Ring bits stay 7, bit 31 (the @ indirection
  flag) clear, `Machine::copy_segment` round-trips it unchanged
  (0x08000000 | 0x70000000), page number 0x1E0000 fits `blocks[]` /
  `permissions[]`. Sparse page map: the block table costs one Block;
  nothing below or above is disturbed.
- **Ring 6 REJECTED.** `Machine::eagle_x_resolve_indirect` applies
  `copy_segment(pc, ac[2|3]+disp)` on the short X-form (the common
  `[ac3+d]`) — a 0x6xxxxxxx base would silently re-ring to 7. Long
  L-forms do not re-ring. Not worth an emulator adjustment.
- **Nothing maps at or above 0x78000000 today.** Ring-7 facts from
  c_src/log (QUEST client): unshared image pages 0–5 (stack base
  0x70000E87), shared image 0x70017C00–0x7017FFFF, and the three
  ?SPAGE windows — SHARED_DATA_FILE at **0x70180000** (126 pages, to
  ~0x7019F800), WORLD_DATA_FILE at 0x70017C00 (1033 pages),
  CASTLE_DATA_FILE at 0x7012AC00 (63 pages). ?SSHPT extends the
  shared partition at startup (this is why 0x70180000 is legal). ?MEMI
  is dead in the live configuration. QUEST_SERVER lays out
  differently (SD at 0x7003F400; windows at 0x70180000, 0x70152400)
  — same Memory class, irrelevant to the client.
- **Static layout, mapped at launch.** NOT lazily mapped (ruling):
  the address book fixes every routine's area; the clone maps exactly
  the book's page count at launch, RW, no exec. A stray access outside
  the laid-out region faults loudly instead of minting a page.
- **Census = confirmation, not search** (ride-along step): an
  ever-mapped bitmap in `Memory::map_page` + a `pagemap` trace line
  per map/unmap/?SSHPT/?MEM(I) + shutdown dump, run under the battery;
  plus a static sweep of quest.dis/quest.mem for absolute addresses
  ≥ 0x78000000. Expected result: empty.

## 2. Scope facts (from the disassembly, verified this session)

- **74 named game routines**, every one reached only by
  `LCALL [abs],N`, arg count on the instruction, every target a
  symbol (0 unnamed LCALL targets in the game range).
- **49 unnamed WSAVS entries** = nested procedures, reached by
  `XCALL 0,N,[pc+rel]` (63 sites). NOT via the saved-wfp chain: the
  **static link is passed in a register** — call site `WMOV 3,1`
  (child of the caller) or `XWLDA 1,[ac3+0x7FFA]` (sibling: reload own
  saved link from the WSAVS ac1 slot) or `WMOV 2,1`; child uses ac1 /
  `WMOV 1,2` and addresses parent locals `[ac2+d]`. All 63 sites
  conform. Consequence: nested routines and their parents can migrate
  INDEPENDENTLY — the link is just a base value that round-trips
  through the WSAVS image. Sequencing is free.
- **Entry variants in game code: WSAVS (124) and WSAVR (2:
  LOCK_FILE, UNLOCK_FILE).** No WSSVS/WSSVR, no XPSHJ callee frames.
  Both push the same five-wide image; one hijack code path.
- **Dynamic stack use** (per-routine census; WSTB is store-byte, NOT
  a stack op — excluded): WMSP/STASP/LDASP (real dynamic locals) in
  13 routines: DIED, DISPLAY_MAGIC, DISPLAY_CAVE, DISPLAY_SCREEN,
  DISPLAY_INVENTORY, GET_QUEST, INIT_OBJ_TBL, OBSERVE, and nested
  ones under HELP, LIST_PLAYERS, OP_HELP(3), STORE. WPSH/WPOP
  balanced pushes in ~15 more.
- **WAVE ONE = PURE ONLY (ruling, conservative):** no WMSP/STASP/
  LDASP AND no WPSH/WPOP. ~74 of 126 entries qualify. The rest appear
  in the book COMMENTED OUT (visible, flippable later).
- Mixed arg counts: REFRESH_SCREEN (0/1), RETURN_MESSAGE (3/6). Tool
  flags them; sizing uses the max.

## 3. The address book

Generated by `tools/build_address_book.py` over Disassembled/quest.dis
+ quest.symbols. Deliverable is `quest.addrbook` (input to the run,
`QUEST_ADDRESS_BOOK=<file>`), plus a side report (call sites per
routine, flags, exclusions and why).

Per routine: entry pc, name (nested: `PARENT.n@<pc>` — enclosing
symbol by address), variant, WSAVS frame size, **max argc over all
call sites**, call-site count, dyn/push flags, and the layout — **a
verbatim frame image, formatted exactly like the master's stack
frame** (ruling):

```
alloc_base                    (16-word aligned)
  args:  max_argc wides       arg N at wfp-10-2N (pointer wides)
  LCALL frame word (wide)     (psr<<16)|argc at wfp-10
  restore image (5 wides):    ac0 @ wfp-8, ac1 @ wfp-6 (= static
                              link for nested), ac2 @ wfp-4,
                              prev wfp @ wfp-2, ac3|carry @ wfp+0
  ← wfp (ac3 points here)
  WSAVS space: frame_size words
next alloc_base
```

(Offsets from EagleStack WSAVS/WRTN + Machine::wide_push: a wide
occupies [wsp..wsp+1] after `wsp+=2`.) Sizing = max_argc*2 + 2 + 10 +
frame_size words, rounded. Bases assigned sequentially from
0x78000000; header records total page count. One line per routine,
hand-editable and diffable:

```
# entry     name              alloc_base  wfp_base   argc frame variant flags
7016E103    INIT_SCREEN       78000000    7800000C   1    0x05  WSAVS   -
#7016603D   DIED              ...                                        dyn
```

Absent/commented = stays stacked. **The book IS the opt-in table**:
routine-by-routine migration under lockstep, bisection by halving the
book, stragglers may stay stacked indefinitely.

## 4. The hijack (clone only; game code untouched)

**Two instructions hijacked: WSAVS/WSAVR and WRTN.** Both keyed off
the book (WSAVS by pc; WRTN by "is wfp an area address").

- **Hijacked WSAVS/WSAVR** (pc in book):
  1. Read the LCALL frame word at [wsp] for the ACTUAL argc (RTBridge
     already reads it). Copy the args (argc wides) + the frame word
     from the real stack into the area at the same offsets. Mirror is
     sized for max argc; unused slots keep prior content.
  2. Push the five restore wides (ac0, ac1, ac2, wfp, ac3|carry)
     **into the area**, not the stack. The real stack is untouched:
     it keeps only the args + frame word the caller pushed. (Earlier
     "WSAVS 50 executes as WSAVS 0" sketch superseded — no restore
     image on the stack at all.)
  3. wfp = ac3 = area wfp. LDAFP 3 refreshes it stock after calls.
     ovk per variant as today; zero_claim on the WSAVS space per
     ruling 8.
  Not in book: exact current behavior.
- **WRTN: stock + one wsp fixup, gated by "wfp is an area address"
  (range test on the register, NOT a pc map — ruling).** Traced
  against the verbatim layout, stock WRTN produces every VALUE
  correctly from the area image: `wsp = wfp` lands in the area, then
  ac3|carry, prev wfp, ac2/ac1/ac0 (patches included), and the frame
  word all pop from the area at the stack offsets. The only wrong
  output is the final wsp (area wfp-10-2*argc). Fixup: after the
  stock sequence, `wsp = real_wsp_at_WSAVS - 2 - 2*argc` — discard
  the frame word + args the caller pushed on the real stack; the live
  record already holds real_wsp_at_WSAVS. Everything else stock. The
  same range test is the discriminator for anything that migrates
  later (WMSP locals, nested frames): "is this an area address," not
  "which pc am I at." The book is consulted BY PC only at WSAVS,
  the one moment a choice is made.
- **Slot-patch return convention is REAL in game code** (user's
  catch): 18 routines store return values into the saved-ac0 slot
  (`XNSTA 0,[ac3+0x7FF9]` = low word of ac0 @ wfp-7, or `XWSTA
  0,[ac3+0x7FF8]` full wide) — OWNS, RANDOM, DIST,
  DISTANCE_TO_PLAYER, DEFEND, LOOK, BOAT, SEIGE, MOVE_PLAYER,
  MOVE_IN_CAVE, and nested procedures under AUTO_MOVE, BARGAIN, BOAT,
  LIST_PLAYERS, LOGON. Same convention as ?UDIV32. Satisfied by
  construction: the area image is the restore source.
- LDAFP, XPEF/LPEF, LCALL, the caller side: unmodified. "Previous
  wfp" is a value the image round-trips, so the caller's base (stack
  or area) is restored without knowing anything changed. Mixed mode
  composes with no extra machinery.
- Static links (nested procedures) unchanged: register values that
  live in the saved-ac1 slot of the area image.
- The area frame and the master's stack frame are THE SAME BYTES at
  different addresses — a future memory compare is a straight T().
- Boot-gate note: a migrated routine never trips the wsp==wsl
  overflow; only I.INIT relies on it and that is runtime, not game.
- Per-routine live flag set/cleared at hijacked WSAVS/WRTN = the
  dynamic re-entrancy tripwire (abort_world on violation).

## 5. Checker Generation 4 — RULINGS

**Premise (user):** the clone's stack may look COMPLETELY different
from the master's — wave one only hijacks WSAVS, but the checker is
built for the general case from day one. The invariant is not "clone
stack = master stack minus some frames"; it is: **every clone address
that the master would hold as a stack address must be translatable
to the master's value.** All of this work lives in the clone.

- **Shadow master-wsp SIMULATION.** The clone runs the master's stack
  arithmetic in parallel, independent of what its own stack does:
  every instruction that moves wsp on the master (WSAVS/WSAVR by
  full size, WRTN, WPSH/WPOP, WMSP, XPEF/LPEF, LCALL, the condition
  system's cuts on unwind — native I.GOTO already owns the wsp reset)
  updates shadow_wsp by the MASTER's amount. This is Plan.md's
  "de-stackify the storage, keep the accounting", made the actual
  mechanism.
- **Live allocation table.** Every clone allocation, wherever it
  physically lands (area, real stack, anything), records
  `(clone_addr, shadow_master_addr, size)` on hijacked WSAVS (one
  record per live area frame: alloc_base → the master's frame base at
  the shadow wsp) and drops it on hijacked WRTN / unwind. Translation `T(clone_value)`: if the
  value falls in a live record → `master_addr + (value − clone_addr)`;
  else identity.
- **Register rule at every pair, all four ACs + carry:** pass if
  `clone == master` OR `T(clone) == master`. wsp: pass if
  `shadow_wsp == master wsp` — the strongest self-check: any missed
  accounting fails every subsequent pair.
- **Mediation (syscall packets, shared writes, L2 crossing entry
  state):** contents compared STRICTLY; pointer-shaped fields
  translated through T before comparing (field-by-field where the
  packet layout is known — the mediator already reads packets by
  field name — word-by-word range test for opaque regions). Escaped
  local pointers (`XPEF [ac3+n]`), static links, buffer addresses in
  ?READ/?WRITE, ac2 packet pointers: all covered by the same T. The
  clone executes its own syscall locally, so result copies already
  land at the clone's addresses.
- pc streams and instruction counts stay STRICT and IDENTICAL (code
  unchanged) — the big win over M3's native spans. All L1 fabric and
  L2 crossings pairs unchanged.
- Recognizing addresses: range test against the book / live records
  suffices — no legitimate game integer lands in 0x78xxxxxx.
- CheckerHistory.md gets a Generation 4 entry from this section.

## 6. Open items (flag in the prompt, settle when reached)

- Unwind: shadow_wsp reset and live-table drop when native I.GOTO
  cuts the stack — the hook point exists (native L2 owns the cut).
  I.GOTO's real-stack cut on the clone must also account for area
  frames having no restore image on the stack (only args + frame
  word per depth).
- Any OTHER instruction that consumes wfp as a stack address (WPOPB?
  I.EPILOG's frame pop, RT residue reads via wsp) — the session must
  grep EagleStack/runtime for every reader of `machine.wfp` and rule
  on each, the same way WRTN was caught here.
- The 18 slot-patchers are FIRST-CLASS validation targets, not
  hazards to avoid: OWNS/RANDOM/DIST early in the widening order.
- Whether the master needs any per-frame check beyond wsp equality
  in wave one (leaning no).
- Mixed-arity residue in the args mirror (REFRESH_SCREEN,
  RETURN_MESSAGE): observe, don't pre-solve.

## 7. Sequencing for Project 12

1. Tool: `build_address_book.py` → `quest.addrbook` + report.
   Review the pure list with the user.
2. Census confirmation (§1) — one battery run, dump attached.
3. Emulator: book loader + launch mapping; hijack in EagleStack;
   shadow wsp + live table + T; register rule; mediation hook.
4. **First routine, startup-reached, pure, no nested children**:
   INIT_SCREEN (frame 5, argc 1, 19 sites) or READ_IN (frame 6,
   argc 0, 33 sites — has an ON-unit, so also exercises I.PROLOG/O.ON
   with an area wfp). Login + ESC at 0 divergences.
5. Widen: OWNS / FIND_OBJECT / RANDOM (hot leaves), then the rest of
   the pure list, batches under the full regression battery
   (M-trigger, FAIL_OPEN, inject shapes 1–3, :ABORT, long free play).
   Bisect by commenting the book.

## 8. Amendments after Project 12 (Aug 15 2026, reviewed and ratified)

Project 12 migrated READ_IN at 0 divergences (docs/Project12/REPORT.md).
These amendments are now part of the design of record.

- **RULING A — L2 walks compare in master coordinates.** Four native
  L2 chain walks test well-formedness by ADDRESS ORDER of the saved-wfp
  chain (r_signal.cpp walk, def_on.cpp pre-walk, frames.cpp i_goto
  unwind walk, native_error_handler::cut). An area frame (0x78..)
  below a real frame (0x70..) reads ascending. Ruling: every comparison
  the master makes in stack coordinates the clone makes in
  T()-translated coordinates; ONLY the comparison operands changed.
  Under a book the clone is native/check-only (the mv attic cannot run
  area frames). Exercised by the QUEST_INJECT-inside-READ_IN run.
- **T covers the real stack above a hijacked frame** (cumulative shift
  10+2f words per live area frame at or below the address); the live
  table carries the cumulative shift. **T⁻¹ at mediation**: the master
  reads its address, verifies the clone at T⁻¹; write replay lands at
  T⁻¹. "Clone executes locally" is LOCAL-calls-only.
- **T_any**: pointer-shaped fields come in three readings — word
  address, @-flagged word (bit 31), BYTE address (0xE0.., word = v>>1);
  both non-word forms were hit by ?READ's packet on the first live run.
- **Sizing correction**: the WSAVS operand counts WIDES; WSAVS space =
  2*frame words; area = 2*max_argc + 12 + 2*frame words, 16-aligned.
- **frames.cpp::wrtn** (I.EPILOG's return, I.GOTO's cut) was a second
  STRUCTURAL wfp consumer, missed in §4, found by the Stage-2 audit;
  takes the same fixup. `area_unwind_to(target)` drops live records
  above the cut; no explicit shadow reset needed.
- **shadow_wsp is built in CLOSED FORM** (T of the clone's wsp with a
  ≥ threshold: master wsp − clone wsp = Σ(10+2f) over live hijacked
  frames at or below wsp). Accepted for wave one — exactly right while
  the only clone/master stack difference is hijacked images, and
  self-checking. MARKER: it embodies "clone stack = master minus
  images", not the general §5 premise; when anything else moves (WMSP
  locals, nested-frame tricks) it must become the per-instruction
  simulation.
- **`nosites` entries are NOT migration candidates until ruled on.**
  29 entries have no LCALL/XCALL caller: QUEST (boot), C_A_LISTENER
  (?CREATE_TASK entry), TERRAIN_HELP, and 26 nested = the ON-UNIT
  BODIES (`XLEF 2,[pc+5]` registration shape, entered by O?SIGNAL
  DISPATCH, not by a call — the hijack's "[wsp] = LCALL frame word"
  read is meaningless there). Tool must flag them (`nocall`) and the
  wave-one filter must exclude them.
- Census facts corrected by the tool: 81 named (the design's 74 used a
  0x7017D000 cutoff; TOWER_ATTACK, TRANSPORT_TERRAK/SUNDAR,
  WRITE_OBJECT, UPDATE_SCREENS, C_A_LISTENER lie above), 49 nested,
  130 entries, 128 WSAVS + 2 WSAVR, 16 slot-patch entries in 14
  symbols, 16 dyn segments; pure = 95 (before the nocall exclusion).
  Nothing maps ≥ 0x78000000 (census dump: 1662 pages, all in
  0x70000000–0x7019F7FF).

## 9. Amendments after Project 13 batch 2 (Aug 15 2026, ruled)

- **RULING — address-book base moves to 0x74000000.** Project 13
  found (boundary 2) that with base 0x78000000 an AREA BYTE POINTER
  (`0x78… << 1 = 0xF0…`) is prefix-identical to a REAL-STACK @-WORD
  (`0x80000000 | 0x70… = 0xF0…`); §8's ordered-guess T_any then
  mistranslates area byte pointers (LOGON's ac2 at ?WRITE_SCREEN,
  GET_INPUT's ?READ packet field). Reordering (their R1) is UNSOUND:
  a real @-word's `>>1` can land inside a live area (P12's own
  0xF0001152 >> 1 = 0x78000A29 ∈ BACKPACK's area). Root cause: bit 27
  set in the base puts the shifted form on the @-flag band. With base
  **0x74000000** every form is disjoint by prefix:
  real word 0x70 / area word 0x74; real byte 0xE0 / area byte 0xE8;
  real @ 0xF0 / area @ 0xF4. Ring bits 7, bit 31 clear,
  `copy_segment` (mask 0x0FFFFFFF) and `set_byte_segment` (mask
  0x1FFFFFFF) preserve the distinguishing bit; census: nothing mapped
  anywhere near. Book FORMAT unchanged; header base + regeneration.
- **DESIGN INVARIANT (now stated):** the pointer forms of area
  addresses must be separable from every real-stack form BY PREFIX;
  that is a constraint on the base. **T_any becomes prefix DISPATCH,
  not ordered guessing**: decode the form from the top bits, translate
  the underlying word (area record or real-stack shift), re-encode the
  same form. Deterministic.
- Regression targets: the two saved divergence signatures
  (Project13/evidence/div_b2_*.txt) must pair.
- **Rename (housekeeping):** "hijack" → "redirect" (WSAVS redirect /
  area redirect) in prose, trace type `hijack` → `redirect`, and code
  identifiers as the touched files allow. Design doc prose is not
  retro-edited except this note.
- Ratified from Project 13's Stage 0: `nocall` flag + exclusion;
  `gcalls` trace (both roles, pure observer at the LCALL/XCALL site)
  with the standing invariant gcalls == redirect-WSAVS count per live
  routine; the `play` driver grown in place. Batch 1 (READ_IN, OWNS,
  FIND_OBJECT, RANDOM, DIST, DISTANCE_TO_PLAYER) LIVE-VALIDATED at 0
  div under the full battery.

## 10. Amendments after Project 13 batch 2 landing (Aug 15 2026, ratified)

- Batch 2 (45 live) LANDED: base 0x74000000, prefix-dispatch T_any,
  both §9 signatures pair, full battery green, batch-1 book re-proven.
- **RATIFIED as rulings:** (a) end-inclusive attribution in T — a
  one-past-the-end pointer belongs to the frame it walked off — with
  the layout invariant `block_end < next_base` honored in the STRIDE
  (bases advance by size+16; the "round exact multiples up" drafting
  was vacuous and is dead). (b) The §9 form taxonomy applies to the
  INVERSE path: mediated reads/replays that receive @-flagged (bit-31)
  addresses mask, invert, re-encode (P13 §6.5). (c) T's real-stack
  shift leg is BOUNDED above by wsl (P13 §6.4 observation, now ruled).
- **docs/Mapper.md is now the design of record for the mapping layer**
  (first-principles spec: bijection A, codec E, single public V,
  invariants I1–I5, three mutation sites, task assert). §5/§8/§9's
  mapping prose remains as history. Project 14 Phase A implements the
  spec and deletes the accreted T/T_any/T_inv; Phase B resumes
  batch 3.
- Task facts corrected on evidence: ?TASK (0500) IS implemented and
  fires twice per session (C_A_LISTENER, entry 0x7017E784, I.ALLOC'd
  stack ~0x70017650, own thread + own Machine). Mapper state is
  per-Machine, so the listener's is empty by construction; the
  main-task assert at redirect makes the precondition loud. CA
  interrupt delivery remains unimplemented; ?REC blocking is
  deliberately fatal.

## 11. Project 14 Phase A (Aug 16 2026, reviewed and ratified)

Mapper refactor landed per docs/Mapper.md (which now carries the P14
rulings: THREE-call surface with frame_precedes, the Q2 interior/
overlay precision, the wsl latch, wave-scoped validity conditions).
Corrections ratified: R-C — the tool's wfp_base was +2 off the §3
design layout; 15 book entries were 2 words short of true extent, and
every prior green battery wrote 2 words into the stride gap (the I1
stride masked a §3 violation until arithmetic caught it — near-miss
recorded, extent-fits-block assert added at configure AND push).
QUEST_INJECT made fail-loud (malformed spec previously no-opped into a
plausible green run; caught by endpoint pinning). Methodology:
wall-clock enters via mediated ?GTOD; world-downstream counts are
BANDED (matrix restated; Run.md §8). Main-task assert is TASK IDENTITY
(header ships wsb==wsl, bounds form unavailable). Regression: 14 runs,
0 div, probes 0, all signatures paired; closed-end point currently has
no live specimen (first-exercise log line in place).

## 12. Project 14 Phase B2 findings + rulings (Aug 22 2026)

B2 widened the live book from 45 to 101 (all callable game routines;
nocall→M5). m/inj/abort validate on the widened book (0 div, anchors
exact, endpoints correct). Two mapper-invariant breaks surfaced and
were correctly STOPPED at boundary 2 (Mapper untouched):

**Finding A — DISPLAY_SCREEN (dyn frame): I4 break — RULED as a MAPPER
STACK-LEG BUG, fix the mapper (do NOT exclude the routine).**
The routine does WSAVS N (executes as WSAVS 0 on the clone: linkage on
the real stack, N-word frame in the area), then MSP grows a dynamic tail
BELOW its linkage marker on the real stack. That tail belongs to
DISPLAY_SCREEN's activation and must translate to the corresponding spot
below DISPLAY_SCREEN's MASTER frame. The bug: the stack leg
(Mapper.cpp ToMaster, `for records innermost-first: if s > W return
s + shift_after`) attributes the tail address to the WRONG frame and
applies a blanket compression shift, lifting s=700010E6 up by 53 wides
to 70001150 — INSIDE DISPLAY_SCREEN's own master extent [7000114E,
700015A2). ToClone then resolves that master address to the AREA (its
`lo<=s<hi` branch), so the directions disagree — I4 breaks. 0
divergences throughout; the game ran identically, only the mapper's
self-check trips.

THE FIX (the correct stack-leg map): attribute a stack address to its
**least-upper-bound live frame** — the frame whose marker W is the
SMALLEST one still >= s (stack grows down, so a frame's MSP tail sits
BELOW its own marker; the owning frame is the nearest marker ABOVE the
address, not below it). Translate s as its offset within that frame's
activation, landing at the corresponding offset below that frame's
master marker (the tail is contiguous below the frame on the master —
same activation, nothing interleaved). ToClone uses the same
attribution so the round trip closes. This is a SEARCH for the least
upper bound, not `shift_after` arithmetic — the closed-form shift was
only ever an approximation that held until a routine put a dynamic tail
below a relocated frame.

Scope: this is a Mapper design refinement (boundary 2, needs care — get
the master-side offset arithmetic exact, fix BOTH directions, verify
against the DISPLAY_SCREEN m leg and the other large dyn routines).
DISPLAY_SCREEN stays LIVE; the mapper is corrected. This likely also
subsumes the "record-order not address-order" note (Mapper §3b) for the
stack leg. NOT an M4c deferral, NOT a routine exclusion — the earlier
draft ruling (exclude to M4c) is SUPERSEDED.

**IMPLEMENTED + GEOMETRY CORRECTED (Aug 22, REPORT_FINDING_A_FIX.md).**
The fix landed and is verified (m/inj/abort/play GREEN on the full 101
book, DISPLAY_SCREEN LIVE). BUT the mechanism narrative above was built
on a DOWNWARD-growing stack, which is WRONG: this machine's stack grows
UP (Machine::wide_push does wsp+=2; WMSP adds). There is no "dynamic
tail below the frame." The real bug: both crashes had s == W exactly —
a register holding the frame's OWN anchor (a position value, captured
via LDASP, the dyn idiom), and the strict `>` skipped the record's own
entry, applying the OUTER record's shift into the frame's master band.
The fix is one threshold: ToMaster stack leg `s > it->W` → `s >= it->W`,
making it agree with shadow_wsp (which always used `>=` for this exact
reason). The "least-upper-bound search" collapses to the `>=` threshold
on an up-growing stack — no search, no new fields. The mapped value now
MATCHES the master (0 div), not merely avoids the abort. A new Q2-style
merge point appears at the band's other edge (W and the area's last-local
both preimage master_wfp+2*frame); resolved by the same rule (inverse →
data → area), ToClone unchanged. So DISPLAY_SCREEN stays live at 101;
the RULING (attribute to owning activation, close the round trip, both
directions) stands — only its geometry is corrected here.

**Finding B — fail-open wsl −14 motion: CHARACTERIZED + RULED
(Aug 22, REPORT_FINDING_B.md).** Outcome 1 — LEGITIMATE real-machine
behaviour, not a slip. wsl on this runtime is DUAL-PURPOSE: stack limit
AND stack/heap fence. The fail-open path moves it in its HEAP-FENCE
role: ?LIB_ERROR calls I?ALLOC (real STASL 0x7017E903; native
i_alloc.cpp:191) to carve a 14-word class-3 block off the top of the
stack segment for the error-message buffer (":USER_DATA_FILE", 15 bytes
→ request 9 → class-3 size 14; wsl 7001715A − 0xE = 7001714C, exact).
The size is a function of MESSAGE LENGTH, not any handler frame — the
brief's "signal frame / re-latch" framing was wrong. The buffer is
retained (task slot B+0x3) for ?ERMSG, freed only at the next
?LIB_ERROR, so wsl stays moved indefinitely — no transient-window
argument saves I2. Both engines run the write (0 divergences); the
abort pc 7017EC7C is the DETECTION site (the game ON-handler's
LJSR I.GOTO → unwind_to → i2_assert), one dispatch after the write.

RULING: I2's INTENT (the stack leg's domain bound must not silently
shift under live records) is right; its IMPLEMENTATION ("wsl constant
while records live") over-approximates, forbidding legitimate heap
allocation during signal handling. Adopt the report's §5 refinement:
**(1) latch `wsl − heap_break` (0x700001F0), not wsl** — I?ALLOC/I?FREE
move both by the same size in the same direction, so the difference is
the true invariant of legitimate heap motion; it still catches a wsl
write with no matching heap-break motion (the real corruption I2
guards). **(2) plus a stack-clearance bound at each i2_assert:**
`wsl > max(live wsp, every record's master-side extent hi)` — the
reclassified band [new_wsl, old_wsl) must sit strictly above all
stack-leg activity so the compression/identity split stays well-defined.
ADJUSTMENT (planning session): bound (2) must be checked against the
POST-Finding-A stack-leg extents — DISPLAY_SCREEN's shadow_wsp (70001FD0)
is the case that most crowds it — so Finding A's `>=` fix and this
heap-fence latch must be VALIDATED TOGETHER on the fo leg, not
independently. Not adopted: tolerating arbitrary wsl deltas; window
re-latch (buffer persists past any window); commenting routines
(record-agnostic, proven useless).

**B2 landing:** m/inj/abort green on the 101 book with DISPLAY_SCREEN
commented for diagnosis; the real landing is 101 live once the stack-leg
mapper fix (Finding A) is in. fo blocked on Finding B's characterization. Play handoff (Stage 3)
proceeds once fo is clean. CheckerHistory Gen-4 append is CORRECTED:
"dyn/push symmetric" holds for divergence but NOT for mapper bijectivity
under large dynamic descent (Finding A) — append after both findings
resolve.
